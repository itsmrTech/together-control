/*
 * Devices Messages
 *
 * This contains all the text for the Devices container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Devices';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Devices container!',
  },
});
