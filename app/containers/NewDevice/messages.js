/*
 * NewDevice Messages
 *
 * This contains all the text for the NewDevice container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewDevice';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewDevice container!',
  },
});
