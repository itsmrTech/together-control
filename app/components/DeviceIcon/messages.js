/*
 * DeviceIcon Messages
 *
 * This contains all the text for the DeviceIcon component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeviceIcon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeviceIcon component!',
  },
});
