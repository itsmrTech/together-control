/*
 * DeviceStatus Messages
 *
 * This contains all the text for the DeviceStatus component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeviceStatus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeviceStatus component!',
  },
});
