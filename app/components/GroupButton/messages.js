/*
 * GroupButton Messages
 *
 * This contains all the text for the GroupButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GroupButton';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GroupButton component!',
  },
});
