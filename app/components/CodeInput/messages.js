/*
 * CodeInput Messages
 *
 * This contains all the text for the CodeInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CodeInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CodeInput component!',
  },
});
